namespace Quaridor
{
    public enum CommandType
    {
        None,
        Move,
        PlaceWall,
    }

    public enum RotationType
    {
        Vertical,
        Horizontal,
        Both
    }
}